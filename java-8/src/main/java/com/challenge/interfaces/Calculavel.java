package com.challenge.interfaces;

import java.math.BigDecimal;

public interface Calculavel {

    BigDecimal somar(Object classe);

    BigDecimal subtrair(Object classes);

    BigDecimal totalizar(Object classes);

}
